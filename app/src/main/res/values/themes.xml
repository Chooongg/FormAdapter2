<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FormAdapter" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.FormAdapter" parent="Base.Theme.FormAdapter">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>